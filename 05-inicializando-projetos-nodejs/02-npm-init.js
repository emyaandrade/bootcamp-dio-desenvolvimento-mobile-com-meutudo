//como criar um organismo JavaScript onde um arquivo conversa com o outro?
//Para isso eu preciso ter um arquivo coração chamado pakage.json na raíz da pasta
//o package.json é o arquivo de configuração do meu projeto

//uma das tarefas que o NPM faz é iniciar o projeto node
//para iniciar o npm basta ir no terminal(usando o atalho: Ctrl + ') e digitar os comandos:
//npm init (vai inicializar o npm)
//depois disso vai aparecer os lugares para digitar informações como por ex:
//package name(nome do pacote), version(versão do projeto), description (descrição do projeto), entry point (ponto de entrada, arquivo principal, geralmente usamos index.js), test command, git repository, keywords(palavras chave do seu projeto), author, license. Caso não queira definir essas infrmações agora, é só apertar Enter e alterar depois.
//depois disso, ele cria o arquivo package.json na raíz do projeto (pasta)

//para alterar as informações que preencheu (ou deixou em branco) acima, é só ir no arquivo package.json e alterar

//Todo arquivo node, como eu sei que é um arquivo Node? Sei que é um arquivo Node porque ele tem um arquivo package.json para criar o projeto e fazer todos os arquivos ali conversar entre eles.

//npm significa Node Package Manager, ou seja,gerenciador de pacotes Node